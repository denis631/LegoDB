(library
 (name executor)
 (libraries
   catalog
   expr
   frontend
   storage

   core
   ctypes.foreign
   llvm llvm.target llvm_X86 llvm.analysis llvm.all_backends llvm.executionengine llvm.scalar_opts)
  (preprocess
   (pps ppx_sexp_conv ppx_hash ppx_compare)))
